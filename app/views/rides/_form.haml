%table{:cellpadding=>'0', :cellspacing=>'0', :width=>'100%'}
  %tr
    %td.label My name:
    %td
      = f.text_field :driver, :class => 'text'
      = f.blank_or_error_td_for :driver
  %tr
    %td.label My email:
    %td
      = f.text_field :email, :class => 'text'
      = f.hint_or_error_td_for :email, "Your email will be anonymized."
  %tr
    %td.label I am leaving from:
    %td
      = f.text_field :location, :class => 'text'
      = f.hint_or_error_td_for :location, "City; neighborhood; whatever's useful."
  %tr
    %td.label I have room for:
    %td
      = f.select :seats, (@ride.min_seats..6).to_a
      passengers
      = seat_hint_for(@ride)
  %tr
    %td.label Anything else?
    %td= f.text_area :comment
    %td.hint
      %p
        Anything else your passengers might need to know. This is a great place to put
        the dates &amp; times you plan to leave and return, even if they are approximate.