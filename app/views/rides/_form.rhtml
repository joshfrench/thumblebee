<table cellpadding="0" cellspacing="0" width="100%">
	<tr>
		<td class="label">My name:</td><td><%= f.text_field :driver, :class => 'text' %></td><td></td>
	</tr>
	<tr>
		<td>My email:</td><td><%= f.text_field :email, :class => 'text' %></td>
		<td rowspan="2" class="hint"><p>Please enter at least an email <strong>or</strong> a phone number so ride-seekers can get in touch. Your personal information will be deleted after <%= @event.name %>.</p></td>
	</tr>
	<tr>
		<td>My phone:</td><td><%= f.text_field :phone, :class => 'text' %></td>
	</tr>
	<tr>
		<td>I am leaving from:</td><td><%= f.text_field :location, :class => 'text' %></td>
		<td class="hint"><p>City; neighborhood; whatever's most useful.</p></td>
	</tr>
	<tr>
		<td>I have room for:</td><td><%= f.select :seats, (@ride.min_seats..6).to_a %> passengers</td>
		<%= seat_hint_for(@ride) %>
	</tr>
	<tr>
		<td>Anything else?</td><td><%= f.text_area :comment %></td>
		<td class="hint"><p>Anything else your passengers might need to know. This is a great place to put the dates &amp; times you plan to leave and return, even if they are approximate.</p></td>
	</tr>
</table>
